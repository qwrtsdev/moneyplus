/// Corner-radius tokens. Kept intentionally small/flat — the design is
/// hard-edged and hairline-bordered rather than the heavily rounded shapes
/// of Material Design.
abstract class AppRadius {
  static const double indicator = 2;
}
